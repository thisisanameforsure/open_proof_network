"""opn_gate — the Open Proof Network gate (D-4): checks, pipeline, attestation.

This package has no authority of its own (D-35). A graph pins the commit of this repo whose gate
runs on it; every verdict is a reproducible artifact (D-5).
"""

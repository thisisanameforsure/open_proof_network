Documented example for the `cursor` connector (F16-R4), copied 2026-09-24. UNVERIFIED: cursor.com
was unreachable from the research sandbox, so `mcp.json` is the example quoted in search snippets
of https://cursor.com/docs/mcp, not a copy of the page. The entry shows "not yet verified" until a
level-2 run against the real `agent` binary passes (F16-AC6, AC9).

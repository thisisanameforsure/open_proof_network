# Golden outputs of the Lean-side metaprograms (F01-AC11, AC12)

Produced by `gate/lean` on the `propositional` fixture under `leanprover/lean4:v4.33.1`.
`witness-types.json`: `opn-witness-type` on each node's `Statement.lean`.
`used-constants-root.json`: `opn-used-constants` on the root's `Proof.lean` with both interior
proofs compiled and the root's Context generated from them (the gate-time substitution, F01-Q4).
Regenerate only when the fixture or the toolchain changes, and say so in the commit.

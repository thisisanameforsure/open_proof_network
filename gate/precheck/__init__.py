"""The precheck job that runs in the scratch repository (F06-R4; D-35).

Not part of ``opn_gate``: it is a script the scratch repo's workflow runs, and it
imports ``opn_gate`` the way any other caller does.
"""

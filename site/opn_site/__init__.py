"""opn_site — the static site generator (D-36).

Empty at F00; filled at F04. Named ``opn_site`` because a top-level package called ``site`` would
shadow Python's own ``site`` module (conventions §1).
"""

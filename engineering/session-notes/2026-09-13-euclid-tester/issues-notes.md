# running issue notes (raw; the report is built from these)

- MCP precheck_submission without token on tutorial node -> {"status":401,"invalid_token"}. MCP-only client cannot mint identity. (w01)
- POST /tokens with empty body -> 400 pseudonym-invalid (misleading; should be "body not JSON"). (w03)
- AGENTS.md / docs page: `schemas/postmortem/v1.json` referenced; graph `schemas/` has only attestation v1,v2 / gate-spec v1 / meta v1; live records use attestation/v4, meta/v3. get_schema("submission-meta/v1") -> not-found although info.json lists submission-meta [1].
- Contributors page: "No ledger files exist yet" though #33 (a partial, credited per AGENTS.md "flat proof line") merged. No ledger files in graph.
- Graph README.md cites architecture_decisions_v_3_10.html; AGENTS.md/info say 3.12.
- Docs page footer: "No license text committed", "No sign-off (DCO) text committed ... D-23 settles it before the first external contributor" — but /dco.json serves DCO 1.1 version f7ac75b443f4ca16 and /tokens requires accepting it.
- Rendered AGENTS.md on docs page: the `output` expectation blocks are shown as bare fragments ("tutorial node:\n/tutorial-and-swap", "picked:", "lean_toolchain: leanprover/lean4:") — reads like broken output.
- AGENTS.md tells: "claims ... first turn nonce into token", filter `claimable` — the only claimable node on the whole network is variant-93e79cb5; walkthrough's `test -n "$CLAIM_NODE"` would fail for most of the time.
- euclid-primes: target listed -> every node non-claimable (claim root -> 404 node-not-in-frontier; claim h1 -> 409 node-not-claimable). Frontier says holes "ready", site node page says "ready to prove", but not claimable; the refusal gives no reason (the targets page does list reasons).
- Root node META.yaml schema meta/v1 while hole META are meta/v3.
- Root attempts/ holds the same partial twice (20260912T110722Z and 20260912T123001Z, identical hash f1ce6c...) — CONTEXT.json assemblies lists both; attempts.count 0.
- h2/h3/h4 statements carry earlier holes as hypotheses they never need (h4 `0 < k.factorial` under three unused hypotheses) — hazard checker `unused-binder` enabled but these admitted; not blocking.
- MCP submit_proof takes `attestation` (the get_precheck result object) while HTTP /submissions takes `precheck_job_id`: two shapes for "one call".
- Site targets page: euclid-primes "fidelity mechanical-only", QA table all "—".
- Site node pages variant-93e79cb5--h1/--h2: "blocked on a dependency" though deps: none (real reason: no witness). Label misleading.
- variant-93e79cb5 is on the frontier with claimable=yes while its deps --h1/--h2 are blocked: contradicts D-25 "ready set (dependencies closed)"; the only claimable node on the network cannot be proved except by bypassing its deps.
- MCP list_frontier filters work (target_id+origin; claimable=true).
- rootA assembly precheck: step 4 fail `dep-unproved` — clear message. But: the service accepted a precheck for a node that is `blocked` and not on the frontier (claim refused 404 node-not-in-frontier) and spent a hosted run to say so; the refusal could be at POST time. Also: "finalizing" a skeleton needs 4 hole PRs merged by a human before the assembly can even be prechecked — no batching; the site/AGENTS.md never says this ordering.
- Precheck attestation for a failing run carries `submitter`, `tooling`, `trust_base` etc. (fine); step 1 diagnostic `mathlib-pinned` is informational on a pass.
- rootB DIRECT proof (references none of the hole theorems) also fails step 4 `dep-unproved`: a merged skeleton locks the root against every proof until all 4 hole PRs merge. Contradicts D-25 racing ("racing is permitted… first merged fills Proof.lean") and AGENTS.md's partial description ("stays open"). A contributor holding a complete proof cannot submit it. Blocks a contributor; gate/protocol.
- variant-93e79cb5 META.yaml `status: blocked` (deps --h1/--h2), yet frontier.json/list_frontier list it with claimable=true (and the frontier page shows claimable yes). Wrong record.
- "Another way" via a D-30 `resolves` variant: propose_variant needs `relation_proof` "the text of a Lean file" — nothing on the site/AGENTS.md says what theorem name/shape/imports it must have, no example exists in the graph (the only variant is `related`), and a proposal opens a PR with no precheck path. Avenue not taken: a malformed PR would be a junk write. Documentation gap.
- get_submission: submit_proof returns submission_id 01M2E406PG0SZ9GVH7Y3E9P6GP; get_submission(that) -> not-found "attestations/01M2E4….json"; get_submission("34") -> not-found; merged attestations are named 000033.json so get_submission("33") -> not-found, only "000033" works. AGENTS.md: "attestations/<pull request number>.json". No GET /submissions/<id> route (404 no such route). A contributor cannot see a pending submission's state through the network at all; only via GitHub (anonymous API: 60 req/h).
- Gate check on every hole PR is red within ~13 s: step 9 (non-author review required) evaluated before the sandbox, so the PR shows `gate … failure` with no build; annotation text explains, but check-run output title/summary empty. A human approved #34 at 19:31:44Z and a pull_request_review-triggered gate run started.
- Precheck hazards on h2/h3/h4: `off-by-one-range at 2 ≤ m` recorded as `hazards-derived-statement`, "recorded and not refused" — on h4 the literal sits in an inherited hypothesis the goal never mentions. Informational; fine.
- GitHub Actions annotation: "Node.js 20 is deprecated … actions/upload-artifact@v4". Cosmetic, gate workflow.
- Attestation 000033 (#33) has model_and_tooling "undeclared".
- 19:37Z, four PRs open against h1–h4: live /frontier.json still shows attempts 0, no claims, for all four; MCP get_node h1: status ready, proof.present false, no mention of an open submission. Another contributor browsing the frontier/MCP sees four untouched holes and would duplicate the work; since claims are refused on a listed target, there is no way to signal "submitted" either. Severity: confusing / wasted work. Where: service/MCP/products.
- Workflow order: step 9 (review) is a workflow step that runs before "Run the gate"; without a review the job exits 1 at step 9 and the Lean is never built, so the PR's red check says nothing about the proof. After approval a new pull_request_review-triggered run builds (6+ min). The contributor cannot request a review through the network (no MCP/HTTP call), only wait.
- #34: gate success at 19:41:38Z with step 9 satisfied (approved 19:31:44Z), still unmerged at 19:44:09Z. D-4 v3.11: "when steps 1–8 are green and step 9 is satisfied, the merge happens with no person pressing a button". The brief/site treat merge as a human act; the protocol says mechanical. Record says one thing, practice another. Severity: confusing. Where: protocol vs gate workflow.
- #34 merged 19:53:45Z by thisisanameforsure (a person pressed the button, 12 min after the green gate at 19:41:38Z), merge commit a9f10904c037. Confirms the merge is manual, not D-4's mechanical merge.
- attestations/000034.json (h1, merged): model_and_tooling "undeclared", tooling {model: null, harness: null}, submitter null, precheck_attestation {hash: null, signature_kind: null} — although submit_proof sent tooling {model: claude-opus-5, version, harness} and the PR body carried the service-signed precheck attestation (job 01M2E3DB606BCKJH3Y9MMYXFR3). D-34 lists model/tooling and the consumed precheck attestation's hash+kind as minimum content. Wrong record. Where: gate post-merge.
- Service GET /frontier.json at 20:05:28Z still rendered_from e559deb (pre-#34) and still lists h1 on the frontier, 3 min after the bot commit, while GET /info.json and MCP get_node already show a9f1090 and h1 proved. The service's two reads disagree; list_frontier is stale. Wrong record (transient?). Where: service.
- Contributors page after the first merged PROOF (#34): still "No ledger files exist yet: nothing has been credited. The first merged proof and the first postmortem will start the ledger". The page's own promise is broken; no ledger file on main. Wrong record. Where: gate post-merge/site.
- Post-merge job took 8.5 min (push run 19:53:47–20:02:21) after a 10 min review-triggered gate run; each hole costs ~20 min of machine time plus a human review, a merge and (for later PRs) a manual "Merge branch 'main'" to satisfy up-to-date rules.
- Watching my own submissions exhausted GitHub's anonymous API budget (60/h) by 20:23Z; reset 20:35:08Z. The network offers no endpoint for a submission's PR/review/check/merge state (get_submission only reads merged attestations; no GET /submissions/<id>), so an account-free contributor (D-19's whole point) is forced onto a rate-limited third-party API to learn what happened to their work. Severity: blocks a contributor (visibility). Where: service/MCP.
- #35 merged 20:13:34Z; at 20:23:37Z no "gate: #35 pass" bot commit and no attestations/000035.json (post-merge for #34 took 8.5 min). Watching.
- attestations/000035.json (h2): same as 000034 — model_and_tooling "undeclared", tooling nulls, submitter null, precheck_attestation hash/kind null. Systematic, not a one-off.

# Tester log: erdos-1050 (outside-contributor agent), 2026-09-21

- 06:22Z start. GET https://openproofnetwork.org/problems/erdos-1050/ -> 200. Page links "How to contribute" -> /docs/#agents, which renders the graph's AGENTS.md. Fast check named on the page: `POST /check`, lean-4.33.1 on AXLE.
- 06:24Z fetched raw AGENTS.md from the graph at cb197ad9 and GET api / (route index).
- 06:23:29Z POST /precheck (anonymous, tutorial-and-swap) -> 202, job 01M31A4GQ8VE288XWJ21EC3RG2, graph_commit 040f0558 (note: graph main is cb197ad9; the precheck pins 040f0558, presumably frontier's rendered_from).
- 06:24:36Z still `running` after 67 s.
- 06:24:40Z SURPRISE: POST /check {"target_id":"erdos-1050","node_id":"erdos-1050--h1-v2","mode":"check","content":"import Mathlib\n\ntheorem witness : True := trivial\n"} -> 200 okay:true but took 22.5 s wall clock (AXLE's own total_request_time_ms = 801). Guide says "The answer comes back in about a second".
  Also lint `helper-declarations` ("a proof is the statement with its sorry replaced...", declarations ["theorem witness"]) fired on a witness text: /check has no notion of "this is a witness", so the lint is a false alarm for the artifact the page tells me to supply.
- Math reading: hole erdos-1050--h1-v2 is Borwein's theorem (1991) itself in approximant form; the merged partial reduced the root to it. Statement has no hypotheses so the witness is `True`.
- 06:26:51Z tutorial precheck done/pass (about 3 min 20 s for a Mathlib-free two-line proof).
- 06:27:46Z POST /tokens -> 201, pseudonym tester-1050-2045, identity 01M31ACBPG88C8V1B2V00D6DBC. Call took 19.6 s.
- 06:28:05Z POST /proposals/witness {"node_id":"erdos-1050--h1-v2","witness":"import Mathlib ... theorem witness : True := trivial"} -> 201 proposal 01M31ACW9R0BEE5DMMVGRBWP9M, graph PR #131. 3.9 s.
- 06:28:14Z POST /claims {"node_id":"erdos-1050--h1-v2","ttl_hours":1} -> 201 claim 01M31AD71G4Q0J887NT12Z7YE6.
- 06:29Z LATENCY: POST /check is bimodal. Identical trivial request (`example : (1:ℕ)+1=2 := rfl`) twice in a row: 2.6 s, then 35.8 s (AXLE reported total_request_time_ms 65 and cached_response true on the slow one). Earlier: 22.5 s and 36.6 s with AXLE time 0.8 s / 0.5 s. /health and /dco.json are 0.5 s. So the stall is in the service, not AXLE. POST /tokens also stalled 19.6 s.
- 06:29Z fast-checked (okay:true, no lint) a lemma: Irrational T -> the hole's approximant form, via Real.exists_int_int_abs_mul_sub_le (Dirichlet). So the hole is *equivalent* to `Irrational T` for the tail T; the merged skeleton moved no mathematical content. File: scratchpad w/conv.lean (text reproduced in the annex I file).
- 06:30:57Z POST /annexes (node erdos-1050--h1-v2, CC-BY-4.0, the equivalence note + Lean) -> 201, graph PR #132, hash 64a620c1...5122. By 06:31:09Z GET /submissions/132 said state closed, merged true, gate success: merged in ~12 s with no human. (waiting_on was null.)
- 06:31Z POST /check mode verify on the hole with a deliberate `done` -> 200 okay:false, goal printed. Works; the goal is printed with default pp (`1 / (2 ^ (n + 3) - 3)`, no ascriptions), so a goal copied from /check into a postmortem is ambiguous about ℝ vs ℕ.
- 06:32:56Z POST /postmortems on erdos-1050--h1-v2 (route_class reduction-to-known, outcome blocked, failure_class missing-library) -> 201, graph PR #135 (so #133/#134 were someone else's in the same minutes; other testers are live). NOTE against myself: the detail says I searched Mathlib by name guesses; I ran that search at 06:34Z, *after* filing. It found nothing (Liouville.irrational exists and cannot apply), so the sentence is true now, but it was written before it was.
- 06:33:25Z BUG: POST /check {"target_id":"erdos-1050","mode":"check","content":"import Mathlib ... example : Irrational (∑' n ...) := by\n  exact?"} -> HTTP 500 body `{"message":"Internal Server Error"}` after 34.6 s. Not the service's structured error shape, no log_id, no Retry-After. Looks like a gateway/function timeout on a slow elaboration. Same file without `exact?` -> 200 in 5.7 s.
- 06:34Z witness PR #131: gate still in_progress, 6 min after opening (guide: "about three minutes on a Mathlib target").
- 06:36:33Z POST /precheck (partial, on erdos-1050--h1-v2) -> 409 node-blocked, cause witness-missing. As the guide says. (Wish: name my own open witness PR #131 in that answer.)
- 06:33-06:37Z postmortem PR #135 merged (GET /submissions/135: closed, merged true).
- 06:37Z FINDING (starvation): witness PR #131 has gone green twice and still is not merged. Runs on its branch: 06:28:08->06:32:17 success (head 01e67e95); main had moved (my annex #132 merged ~06:31) so the branch was updated 06:32:25; 06:32:30->06:34:57 success (677b1592); main had moved again (my postmortem #135 merged 06:33:38, bot commit 06:35:54) so updated 06:35:02; third run in progress. Each round is 2.5-4 min, and every append anywhere on the graph (mine, other testers': #133/#134 are not mine) invalidates the green. `waiting_on` said `gate` throughout, never `branch-update`.
- MCP: POST /mcp tools/list without initialize or bearer -> 200 with the tool list. Fine.
- 06:40:53Z witness PR #131 MERGED, 12 min 48 s after opening, on its FOURTH green gate round (runs 06:28:08, 06:32:30, 06:35:07, 06:38:06; each green, each but the last invalidated by main moving). Guide: "about three minutes on a Mathlib target".
- 06:41:49Z POST /precheck (partial on erdos-1050--h1-v2) -> 409 node-blocked "its witness slot is empty, and a witness goes in through POST /proposals/witness" -- 56 s after my witness merged. No Retry-After, no mention that a witness merged and products are pending. The guide promises `409 products-pending` with Retry-After for a merged *proposal*; a merged witness gets the stale blocked answer telling me to do what I already did.
- 06:41Z site node page for the hole shows my annex (rendered at graph commit 3ef8d6ec), no sign of the postmortem yet.
- 06:43:22Z bot commit `gate: #131 pass`. GET /submissions/131 -> waiting_on null the whole time after merge (guide says a merged proposal reads `products`). /precheck kept answering 409 node-blocked/witness-missing until 06:43:26Z at least; first 202 at 06:44:09Z (3 min 16 s after the merge). Job 01M31BA8QGBV80K81P94STYNQM: partial on the hole (reduce to Irrational T, cites my annex).
- 06:45Z POST /check node_id erdos-1050 (root), mode check and verify, content = the merged root partial with the `borwein` sorry replaced by `erdos_1050__h1` -> okay:false "Unknown identifier `erdos_1050__h1`", inlined_defs []. The root's Statement.lean imports only Mathlib (not its own Context), so the root cannot be closed *through* its hole, only by a direct proof that inlines everything. Guide sentence "With a `node_id`, the node's own `Nodes.«<id>».Context` is inlined as well" did not hold for this node (or holds only when the statement imports it; it does not say).
- 06:47:18Z BUG REPRO: the `exact?` /check again -> 500 `{"message":"Internal Server Error"}`, this time after 15.6 s. Reproducible 2/2.
- 06:47:47Z precheck 01M31BA8QGBV80K81P94STYNQM done/pass (created 06:44:06, so <= 3 min 40 s on a Mathlib target). Steps 1,2,4,5,6,7,8 pass; step 4 names one hole `hT`.
- 06:47:59Z POST /submissions (artifact_type partial, precheck_job_id above) -> 201 submission 01M31BHAA8H454T9SXQKNMMBE8, graph PR #140.
- precheck step 6 on the hole reported (pass, "recorded and not refused") an `unused-binder` finding: "binder a._@._internal._hyg.0 is quantified but nothing after it mentions it" for `∃ a b : ℕ → ℤ, ...` where `a` IS used. Looks like a false positive of the checker on multi-binder `∃ a b`.
- site: my annex is rendered as flowed paragraphs, so the Lean in it lost its line breaks and indentation (I had indented by 3 spaces, no fence). Guide never says an annex is Markdown or how it renders.
- site at graph commit 91448f00 (06:50Z): hole page no longer says "needs a witness"; histogram shows missing-library; my pseudonym appears nowhere on the page.
- 06:48:01Z-06:51:29Z PR #140 gate round 1 green (3.5 min); branch was behind again, updated 06:51:44Z, round 2 running. Same starvation pattern as #131.
- guide gap: `/approach-records` / `submit_approach_record` appear only in the route index and the MCP table; no section says what an approach record is or its body.
- 06:53Z POST /claims on superseded erdos-1050--h1 -> 409 node-not-open naming the replacement (good). POST /check verify on the superseded node -> okay:null, user_error "failed to compile formal_statement" with no word that the node is superseded.
- 06:59:36Z PR #140: three green rounds (06:48:01-06:51:29, 06:51:44-06:55:00, 06:55:18-06:58:12), each overtaken by main moving; fourth round started 06:58:29. 11+ min waiting; moving on per the timebox.
- 07:02:41Z PR #140 still open: FOUR green gate rounds so far, a fifth started (head 8e03f908). Open PRs on the graph at this moment: #137, #139, #140 (three Mathlib-gated PRs leapfrogging each other plus appends). Stopping at the timebox with #140 pending.
- 07:03Z DELETE /claims/01M31AD71G4Q0J887NT12Z7YE6 (released my claim).
- End state: merged #131 (witness, hole now ready), #132 (annex), #135 (postmortem); pending #140 (partial on erdos-1050--h1-v2, one hole `hT : Irrational T`). Source repo NOT read at any point.

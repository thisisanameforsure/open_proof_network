F07-T25 — the merge actor (R9, R10, Q33, closing Q16; D-4)
date: 2026-09-20

## why: D-4 (since v3.11): 'the merge happens with no person pressing a button'. Nothing did. 2026-09-19 primes run: PR #110 gate-green and clean at 18:42Z, merged by hand at 19:14Z; agents A-D idle 21-35 min each behind a green proposal

## the credential question (the owner asked whether an existing OPN_GRAPH_MERGE_TOKEN had been tried): none exists.
gh secret list -R <graph>: OPN_GATE_SIGNING_KEY, OPN_GRAPH_DEPLOY_KEY, OPN_SITE_DEPLOY_TOKEN. .env key names: AWS_*, MY_USERNAME, MY_TOKEN, OPN_API_*. The name was this plan's own invention.
ruleset 22508489: required checks 'gate (steps 1, 2 and 4-8 in the sandbox)' and 'step 9 (a non-author approving review)', strict up-to-date; allow_auto_merge=false; merge queues are not offered to a personal account

## the shipped decision program, extracted from the YAML and run read-only against the live graph:
merge actor: update #107   (number=107 sha=597de448... action=update; #107 and #108 are the erdos-69 tester's open service PRs, both green and behind)

## tests (against graph branch f07-t25-merge-actor until it is on origin/main):
16 passed in 0.17s

## not yet evidenced: a real merge by the actor. It needs the owner's token; until then every run is a dry run by construction.

## 2026-09-20, later: the owner's token, checked before it was installed (its value never printed)
shape: github_pat_…, 93 chars, no whitespace; expiry header 2026-12-31 22:00:00 UTC
inert probes (a head sha of forty zeros can match nothing, so nothing can change):
  PUT pulls/107/update-branch  -> 422 "expected head sha didn't match current head ref"   (authorised)
  PUT pulls/107/merge          -> 409 "Head branch was modified"                           (authorised)
  POST <network repo>/git/refs -> 403 "Resource not accessible by personal access token"   (out of reach: good)
  GET  <graph>/actions/secrets -> 403                                                      (cannot read secrets: good)
ruleset 22508489 bypass_actors: DeployKey always; RepositoryRole id 5 (admin) always; current_user_can_bypass: always
=> the token is the owner's, so it CAN bypass. The design's claim "GitHub refuses anything not green" was false as written.
   Not tested by attempting a red merge: a working bypass would have merged a failed proof (#123).
a second guard considered and measured, then dropped: mergeable_state as seen by a viewer who cannot bypass
  anonymous: #123 (gate red) -> unstable; #107 (green, behind) -> clean     owner: #123 -> blocked; #107 -> behind
  the host does not evaluate the ruleset for an unprivileged viewer, so that signal is wrong, not merely weak.
what guards instead: required check names read from rules/branches/main on every run (no credential needed:
  strict = True, required = the gate job and the step 9 job, by exact name); the post-merge job's re-derivation (F03-Q7).
the revised decision program, run read-only against the live graph: "merge actor: update #107". Tests: 17 passed.

## 2026-09-20, live: the first merges no person pressed (graph, after the push at 07:49Z; secret set 07:15Z)
07:49:42Z merge actor: update #107   -> "updated the branch of #107; the gate re-runs on it and wakes this workflow"
07:52:38Z merge actor: merge  #108   -> "merged #108 at c6737e6"   (the oldest GREEN one while #107's gate was still running)
          post-merge: "gate: #108 pass" (2257ef9)
07:55:24Z, 07:58:00Z                 -> updated the branch of #107 twice more, as main moved under it (the re-render
                                        push d2809a2, then #108's merge and bot commit)
08:00:37Z merge actor: merge  #107   -> "merged #107 at 87fd37b"; gh: merged 2026-09-20T08:00:38Z; post-merge "gate: #107 pass" (c8e5ba6)
Both were an outside contributor's gate-green pull requests (the erdos-69 tester), open since 2026-09-18.
The merges are attributed to the token's owner (mergedBy thisisanameforsure), as C8 says they would be.
Twelve merge runs in eleven minutes, all success; the no-op ones (woken by a gate run with nothing green)
cost about ten seconds each.

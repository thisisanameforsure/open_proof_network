The graph re-pinned to network 3c63578, 2026-09-14 (F14-T15; Mike: "yeah roll it out")

Carries F14 to the live gate:
- a listed target is claimable;
- step 9 is waived by a certificate or by catalog evidence at five points;
- the statement-evidence, formalization, qa/v2 and targets-index/v5 schemas;
- THIRD_PARTY_NOTICES.md as a merge product;
- the owner's rulings: precheck artifact_type, get_node live claims, target pages state claimability;
- decisions v3.15, with PROTOCOL_VERSION 3.15.

0. Before the tag
   - Network main d707b43 (F14 plus the rulings) was pushed. api-deploy 34865340843 succeeded.
   - The guide copy went to the graph first as c6affa1, so the network's copy test compares equal. Then network
     3c63578 (the guide) was pushed with 2100 passed.
   - Rehearsals: the five evidence diffs classify as curator mode with no problems, and erdos-51 and
     riemann-hypothesis classify as intake with no problems (task-15-rehearsal.txt).

1. CI on 3c63578 (run 34866271259): fast tier success; make verify-lean (real toolchain and sandbox) success.
   The superseded run on d707b43 was cancelled.

2. Tag gate-3c63578, pushed 17:02Z. image-publish run 34872317261 succeeded:
   mathlib none                                      ghcr.io/thisisanameforsure/opn-gate@sha256:d8bb6c671584dc45a947e566d4d4e85ce92e39c02e0f3bb5b8326e7e1797f6f7
   mathlib 0df444a360eaa60ab8c11dca51a86af692955474  ghcr.io/thisisanameforsure/opn-gate@sha256:9a3b0387f84a825905693c1a6652fb97d50d91ea466403ba353250d03138f23e
   The Mathlib digest was read from its job's "Push and record the digest" step; the plain digest is the other.

3. Pins, written by gate/tools/pin_image.py (--network-commit 3c63578370f7cc3b9b4666c57492606d385c6873,
   --api-url https://api.openproofnetwork.org). tutorial got the plain image. erdos-52, -68, -172, -376 and
   -406 got the Mathlib image, with euclid-primes last.
   pin_image.py --check: the six Mathlib targets "pin ok". The tutorial has 1 problem (the devcontainer runs
   the Mathlib image), as at every earlier re-pin. 7 of 7 gate-spec.json files name 3c63578.
   In the same commit:
   - schemas.publish added formalization/v1, qa/v2, statement-evidence/v1 and targets-index/v5, pinned in
     schemas/HASHES;
   - .github/workflows/gate.yml gained, after the ledger line:
       [ -f THIRD_PARTY_NOTICES.md ] && git add THIRD_PARTY_NOTICES.md  # F14-R12: a merge product
   Committed as 9fb6b19 on top of c6affa1.

4. Pregate on the committed re-pin, with the gate at 3c63578:
   $ gate/pregate.sh --graph <repin worktree> --target tutorial --node tutorial-and-swap
   exit 0, verdict pass. Steps 1 toolchain, 2 paths, 4 kernel-replay, 5 axioms, 6 hazards, 7 witness and 8 deps
   ran. 54 s, olean cache hit.

5. The graph push: origin/main c6affa1 -> 9fb6b19, and the shared graph checkout fast-forwarded. The push's
   workflow run 34873839057 succeeded (postmerge found no merged pull request).

6. Not yet true, by construction: the committed products still render under the old pin until the first
   merged pull request (the erdos-52 evidence PR) re-renders them as targets-index/v5.

7. First merge under the new pin: graph PR #39 (evidence/erdos-52), merged, with its bot commit on main. The live
   targets/index.json is now targets-index/v5, rendered_from 171533c:
     erdos-172, -376, -406, -68  claimable True []  step9 review  (their evidence PRs #40 onward are still merging)
     erdos-52                    claimable True []  step9 evidence  score 6
     euclid-primes               claimable False ['status-resolved']
     tutorial                    claimable True []
   THIRD_PARTY_NOTICES.md is present on main as a product.
   site-deploy runs 34873855217 and 34874242993 succeeded. https://openproofnetwork.org/targets/erdos-52/ shows
   "open for claims", "Catalog evidence" and "without a human reviewer", and does not show "Not claimable".

8. Wave one: all 22 intake branches (21 Erdős and riemann-hypothesis) were cut from 171533c, imported with
   --no-toolchain and their catalog evidence, and classified as intake with no problems. None touches a
   path outside its own target. All were pushed. They merge one at a time after the evidence PRs, and
   admission runs in the graph's CI on each pull request.

9. The five evidence PRs merged in sequence, each after its bot commit: #39 erdos-52, #40 erdos-68, #41 erdos-172,
   #42 erdos-376, #43 erdos-406. The live targets/index.json (targets-index/v5, rendered_from 4c5187f):
     erdos-52  claimable True  step9 evidence  score 6
     erdos-68, -172, -376, -406  claimable True  step9 evidence  score 8
     euclid-primes  claimable False (status-resolved)
     tutorial  claimable True  step9 review
   The wave merges started at #44 (intake/erdos-1003).

10. Wave, first pass: #44 erdos-1003 and #45 erdos-1065 merged with their bot commits. #46 erdos-1094 failed
    admission at step 6 (run 34877116285):
      hazard-unacknowledged: div-zero at n / k — Lean's / is total and gives a junk value at 0
    The refusal is right, and the division is safe. The statement is
      {(n, k) : N x N | 0 < k and 2 * k <= n and (n.choose k).minFac > max (n / k) k}.Finite
    so 0 < k is a conjunct of the same predicate. For a natural number p, p > floor(n/k) holds exactly when
    p > n/k, so floor division matches the informal max(n/k, k). The curator acknowledged it in
    nodes/erdos-1094/META.yaml (checker div-zero, location "n / k", with that justification), pushed as f9650c6.
    The merge script now holds a pull request whose gate fails and moves on. The remaining 20 are queued, with
    erdos-1094 last.
    Gap: wave.py drafts cannot run the Lean hazard checkers without a Mathlib checkout, so a draft's first
    hazard verdict comes from its own pull request. For wave two: run the step-6 checkers in the Mathlib image
    before pushing, which is T11L's docker-tier admission.

# F14-T13 part 2: a fresh agent on the bench, 2026-09-14

The bench graph was a local clone of the live graph at f874fb2. It carried this branch's schemas, evidence
records for the five Erdős targets, products rendered by this branch's gate, and the guide as T15
will ship it. It was served by `gate/tools/bench.py serve`. The agent was given only the brief,
with erdos-376 as its target. No live host was touched.

The agent's own report follows, as it returned it. Its file write was refused, so it is recorded here.

## Summary

- Writes by status code: 201 × 7 (token, claim, duplicate claim, annex, crux proposal, postmortem,
  partial submission); 202 × 3 (prechecks); 200 × 3 (releases, one a repeat); 400 × 3 (one
  malformed token request of its own, one correct reused-nonce refusal, one partial refused for
  its path). No 5xx, no 429.
- A token after about 0.7 minutes. The pass took about 2 minutes, not the brief's 25.
- Contributions: an annex with the Kummer carries argument; a speculative crux `spec-e6d7c459`; a
  postmortem; a partial skeleton for the root citing the annex.

## Bugs it reported

1. The same token can claim erdos-376 twice; both are 201 and the frontier shows two active claims.
2. MCP `get_node` shows `claims.active: []` while `/frontier.json` shows two.
3. A partial at `nodes/erdos-376/Proof.lean` passes precheck, then `POST /submissions` answers 400
   `artifact-path-mismatch`, wasting the precheck.
4. A repeat release answers 200 with no "already released" signal.
5. erdos-376's hash-pinned `Statement.lean` docstring still says "Listed, not claimable, at Stage 0".
6. The tutorial shows `status: resolved` with `claimable: true` and no explanation, while
   euclid-primes correctly lists `status-resolved`.
7. The site says it was rendered from 659575964beb, while claims and the service use fc38913326b8,
   with no explanation. On the bench this is expected, because the site base and the network
   commit differ.
8. The frontier page says "this build names no service", and the docs page shows AGENTS.md as
   untrusted raw markdown. On the bench, the service URL is not configured into the site build.

## Where the guide, the site and the service disagreed

- **Where a partial goes.** The guide never says where a partial's bundle goes. The only path it
  shows is `Proof.lean`. The correct `attempts/<ts>-<pseudonym>-partial.lean` appears only in the
  400 message. The guide's skeleton imports `Nodes.«some-node».Context`, while the Erdős statements
  import `Mathlib`.
- **Whether a target is claimable.** The target page never says it outright. It shows "listed,
  mechanical-only"; only the frontier says "Claimable yes". The reviewer sentence was clear:
  "merges on the gate without a human reviewer … evidence scores 8 (A)".
- **How fresh the claims are.** `/frontier.json` is live, while MCP `get_node` and the site are
  snapshots.
- **The fast-check version.** The fast check runs lean-4.33.0 against the gate's v4.33.1. This is
  disclosed.
- **The QA table.** In text it renders its `equivalence` column as "—" and never explains it.

## What it could not exercise

- **Second formalizations.** The bench has none on any target, so no equivalence verdict appears.
- **Lean.** Every precheck passes on the bench, and the fake `/check` answered `okay: true` to
  non-Lean text, so none of the agent's Lean was elaborated.

## Replay: `gate/tools/bench.py replay --bench <bench> --assert`, exit 0

| Push | Mode | Outcome |
|---|---|---|
| 1–3 | annex, proposal, postmortem | classified clean and merged onto the replay branch |
| 4 | partial | skipped: needs the gate's toolchain |

Nothing was refused. The products and the site re-rendered in process.

Frontier changes:
- erdos-376 `annex_present` false to true
- erdos-376 `attempts` 0 to 1, with the `informal-gap` histogram entry
- `spec-e6d7c459` ADDED

Pages changed: `docs`, `frontier`, `index`, erdos-376's node page, the new crux's page, and erdos-376's target page.

## Triage

- **Real:** bugs 1–6, plus the partial-path gap in the guide. None comes from F14's own changes. Each is to become a failing `test_finding_*` test, per the plan's QA gate 3.
- **Belongs to F14:** bug 5, the stale "not claimable" docstring. It is inside hash-pinned statements, and the page never states claimability in words. Both go to T15 and T7 follow-ups.
- **Bench artefacts:** bugs 7 and 8.

## Red tests (plan QA gate 3), 2026-09-14

Six test-only agents checked each finding against the protocol and the specs. Eight tests now
fail, and each is committed as a strict expected failure that names its fix. Two more tests pass
and guard the fixes.

| Finding | Verdict | Test |
|---|---|---|
| A duplicate claim by one identity | Real (D-25, F05-R7) | `api/tests/test_finding_bench_claims.py`, two xfails. The fix must also rewrite `test_ttl_caps`. |
| A repeat release answers 200 | Correct as built (F05-R8) | none |
| MCP `get_node` claims empty | By design: top-level `claims` is live, `context.claims` is the merge snapshot (D-35, F10-R4). The only gap is a doc note saying which is live. | none |
| Precheck passes a partial that submission refuses | Real, but a fix needs an optional `artifact_type` on `POST /precheck`, an F06-R1 amendment. Owner's call. | `api/tests/test_finding_bench_partial_path.py`, one xfail |
| A resolved uncurated tutorial reads claimable | Real (D-33, F11-R4). `products.target_facts` returns early without `target.yaml`. | `gate/tests/test_finding_bench_resolved_uncurated.py`, one xfail |
| The target page never states claimability | Real (F14-R10) | `site/tests/test_finding_bench_claimable_words.py`, two xfails |
| The guide omits the partial's attempts path | Real | `gate/tests/test_finding_bench_guide_partial.py`, two xfails |

# Open Proof Network — `network` repo

What this repo is, its sibling `graph` repo, and the rules between them:

@AGENTS.md

How it is built (the build protocol, loaded for every session):

@engineering/CLAUDE.md

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://openproof.network/schemas/mcp/check_lean/v1.json",
  "title": "mcp/check_lean/v1",
  "description": "The result of the check_lean tool (F13-R12; D-28, D-35 v3.14): POST /check's response, passed through in an envelope whether it answered or refused; or an adapter error result. The answer is never authoritative.",
  "oneOf": [
    {
      "type": "object",
      "description": "The endpoint's response, passed through (F09-R7): status, body, and Retry-After when the identity layer or the in-flight cap sent one",
      "required": ["status", "body"],
      "properties": {
        "status": {"type": "integer"},
        "body": {"type": ["object", "array", "string", "number", "boolean", "null"]},
        "retry_after": {"type": "string"}
      }
    },
    {
      "type": "object",
      "description": "The plain path did not answer, or the arguments were refused (F09-R10)",
      "required": ["error", "message", "source"],
      "additionalProperties": false,
      "properties": {
        "error": {"type": "string"},
        "message": {"type": "string"},
        "source": {"enum": ["graph", "service", "adapter"]},
        "status": {"type": "integer"}
      }
    }
  ]
}
